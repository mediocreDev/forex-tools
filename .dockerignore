node_modules
npm-debug.log
pnpm-debug.log*

# Host build output (builder stage creates its own)
dist

# Logs
logs
*.log

# Secrets — never bake these into images
.env
.env.local
.env.production
# NOTE: .env.dev is NOT excluded — Vite reads it during build:dev

# CI/CD & tooling
.github
.vscode
.idea
.git
.gitignore
.dockerignore

# OS junk
.DS_Store
Thumbs.db

# Infrastructure (not part of the app image)
monitoring
docker-compose*.yml
GUIDE.md
